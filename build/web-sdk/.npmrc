# Scope mapping only — deliberately NO _authToken here. A literal ${NODE_AUTH_TOKEN}
# this file's readers cannot resolve would override credentials injected by CI or by
# Dependabot. CI gets auth from actions/setup-node; local dev from ~/.npmrc (see README.md).
@tiro-health:registry=https://npm.pkg.github.com
